<div dir="rtl">

# 🚀 پروژه SimpleFlow: سامانه هوشمند مدیریت احراز هویت

### 📋 معرفی پروژه
پروژه **SimpleFlow** یک راهکار مدرن و فرانت-اند (Front-end) برای مدیریت فرآیندهای ورود و ثبت‌نام کاربر است. این سامانه با تمرکز بر **امنیت داده‌های کاربر** و **رابط کاربری تعاملی** توسعه یافته تا تجربه بی‌نقصی را در هنگام عضویت در پلتفرم فراهم کند.

---

## ✨ ویژگی‌های برجسته فنی

### ۱. اعتبارسنجی زنده رمز عبور (Live Password Validation)
یکی از بخش‌های کلیدی این پروژه، سیستم بررسی هوشمند رمز عبور است. به محض اینکه کاربر شروع به تایپ می‌کند، جاوااسکریپت به صورت لحظه‌ای سه معیار اصلی را بررسی می‌کند:
* **طول کاراکتر:** رمز عبور باید حداقل دارای ۸ کاراکتر باشد.
* **پیچیدگی محتوا:** استفاده از حداقل یک عدد یا نماد خاص در رمز عبور الزامی است.
* **حفاظت از اطلاعات شخصی:** سیستم بررسی می‌کند که رمز عبور شامل نام، نام کاربری یا آدرس ایمیل کاربر نباشد تا امنیت حساب به حداکثر برسد.

### ۲. سنجش قدرت بصری (Password Strength Meter)
براساس امتیازات کسب شده از اعتبارسنجی، قدرت رمز عبور در ۴ سطح نمایش داده می‌شود:
* 🔴 **خیلی ضعیف (Very Weak):** هیچ معیاری رعایت نشده است.
* 🟠 **ضعیف (Weak):** تنها یک معیار رعایت شده است.
* 🟡 **متوسط (Medium):** دو معیار رعایت شده است.
* 🟢 **قوی (Strong):** تمام معیارهای امنیتی رعایت شده است.

### ۳. مدیریت پویا تب‌ها (Dynamic Tab System)
کاربر می‌تواند بدون نیاز به بارگذاری مجدد صفحه (Reload)، بین فرم‌های **Sign In** و **Sign Up** جابجا شود. این کار با استفاده از مدیریت DOM در جاوااسکریپت انجام شده و باعث افزایش سرعت و نرمی کار با سامانه می‌شود.

---

## 🛠 تکنولوژی‌های استفاده شده

* **HTML5:** جهت ساختاردهی استاندارد و دسترسی‌پذیری المان‌های فرم.
* **CSS3:** بهره‌گیری از سیستم **Flexbox** برای چیدمان، فونت سفارشی **Figtree**، و انیمیشن‌های Transition برای دکمه‌ها و فیلدها.
* **JavaScript (ES6+):** پیاده‌سازی منطق محاسباتی امنیت رمز عبور، اعتبارسنجی فرمت ایمیل و مدیریت رویدادهای کلیک (Event Handlers).

---

## 🎨 طراحی و تجربه کاربری (UI/UX)

* **طراحی Split-Screen:** صفحه به دو بخش تقسیم شده است؛ سمت چپ یک پنل خوش‌آمدگویی بصری با تصویر پس‌زمینه جذاب و سمت راست محیط تعامل کاربر.
* **رنگ‌بندی هوشمند:** استفاده از رنگ‌های خنثی برای حالت اولیه و رنگ‌های سبز و قرمز برای نمایش وضعیت اعتبار فیلدها.
* **واکنش‌گرایی (Responsiveness):** استفاده از واحدهای نسبی و Flexbox برای اطمینان از نمایش صحیح در ابعاد مختلف نمایشگر.

---

## 📂 راهنمای نصب و راه‌اندازی سریع

برای اجرای پروژه روی سیستم شخصی خود، مراحل زیر را دنبال کنید:

1.  **ایجاد فایل:** یک فایل با نام `index.html` ایجاد کرده و کدهای پروژه را در آن قرار دهید.
2.  **تنظیم تصویر:** تصویر پس‌زمینه پروژه را با نام زیر در همان پوشه ذخیره کنید:
    `d71a1f0106b82a54e324dce1346501dcbea0e315.jpg`
3.  **اجرا:** فایل را در یکی از مرورگرهای مدرن (Chrome, Firefox, Edge) باز کرده و از خروجی لذت ببرید.

---

## 👨‍💻 اهداف آینده پروژه
* افزودن قابلیت نمایش/مخفی‌سازی رمز عبور (Show/Hide Password).
* پیاده‌سازی حالت تیره (Dark Mode).
* اتصال به پایگاه داده با استفاده از Node.js یا Firebase.

</div>
